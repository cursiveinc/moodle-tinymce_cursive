
    {{$title}}Submit Source {{/title}}
    {{$body}}
        <div class="container">
            <form id="formid">
                <div class="form-group row">
                    <label for="inputEmail" class="col-sm-2 col-form-label"> sourceurl </label>
                    <div class="col-sm-10">
                        <textarea  class="form-control inputUrl" value="" id="inputUrl" placeholder="sourceurl"></textarea>
                    </div>
                    </div>
                  {{!---  <div class="form-group row">
                    <label for="inputEmail" class="col-sm-2 col-form-label"> Paste Text </label>
                    <div class="col-sm-10">
                        <textarea type="url" class="form-control inputUrl" value="" id="pastetext" placeholder="sourceurl">
                        </textarea>
                    </div>
                </div>---}}
            </form>
        </div>
    {{/body}}
    
